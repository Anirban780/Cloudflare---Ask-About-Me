# Plan — Slice S2: Persona Agent & Llama 3.3

> **Slice:** S2  
> **Goal:** Configure Llama 3.3 70B (`@cf/meta/llama-3.3-70b-instruct-fp8-fast`), build owner persona system prompt, implement `PortfolioAgent` Durable Object, eliminate starter demo tools, and rebrand UI to Ask-About-Me.  
> **Companions:** `docs/SPECS.md` §4.1, §8.1, §8.2, §10, §15.

---

## 1. Approach

1. **Wrangler configuration (`wrangler.jsonc`)**:
   - Set `"name": "ask-about-me"`
   - Bind Durable Object `PortfolioAgent` (class `PortfolioAgent`)
   - Configure migration `v1` with `new_sqlite_classes: ["PortfolioAgent"]`
   - Set `assets.run_worker_first: ["/agents/*", "/api/*", "/oauth/*"]`
   - Define environment vars in `vars`:
     - `CHAT_MODEL: "@cf/meta/llama-3.3-70b-instruct-fp8-fast"`
     - `RETRIEVAL_MODE: "tool"`
     - `OWNER_NAME: "Anirban Sarkar"`
     - `OWNER_FIRST: "Anirban"`
     - `AGENT_NAME: "Ask-About-Me"`
     - `GITHUB_USER: "Anirban780"`
     - `TARGET_ROLES: "Software Engineer, Data Engineer, Cloud Engineer, DevOps Engineer"`
2. **System prompt (`src/agent/system-prompt.ts`)**:
   - Create `buildSystemPrompt(env: Env, state?: VisitorState): string` implementing SPECS §8.2.
   - Persona: Third-person AI concierge representing Anirban Sarkar.
   - Grounding rules: Strict refusal to hallucinate; data is untrusted; concise responses.
   - Visitor personalization dynamic suffix: Injects known name, company, hiring role, interests safely.
3. **Portfolio agent (`src/agent/portfolio-agent.ts`)**:
   - Subclass `AIChatAgent<Env, VisitorState>`.
   - Maintain `initialState: VisitorState`.
   - Implement `onChatMessage` using `createWorkersAI({ binding: this.env.AI })` and `streamText`.
   - Model: `env.CHAT_MODEL || "@cf/meta/llama-3.3-70b-instruct-fp8-fast"`.
   - Temperature: `0.2` (low temperature for grounded answers).
   - Maximum output tokens: `MAX_OUTPUT_TOKENS: 600`.
   - No demo tools (`getWeather`, `calculate`, etc. removed).
4. **Worker entry (`src/server.ts`)**:
   - Export `PortfolioAgent`.
   - Route `/agents/*` via `routeAgentRequest`.
   - Add `/api/health` returning status and active model.
5. **Frontend UI (`src/app.tsx`)**:
   - Connect `useAgent<PortfolioAgent>({ agent: "PortfolioAgent" })`.
   - Rebrand header: "Ask-About-Me" — "AI Portfolio Concierge for Anirban Sarkar".
   - Remove demo elements (MCP servers panel, image attachment helpers, demo tool toasts).
   - Maintain clean streaming chat, markdown rendering, theme toggle, and history clearing.

## 2. Files to Touch

- `wrangler.jsonc` (Modify bindings & vars)
- `src/agent/system-prompt.ts` (New)
- `src/agent/portfolio-agent.ts` (New)
- `src/server.ts` (Modify: export PortfolioAgent, route health endpoint, remove demo tools)
- `src/app.tsx` (Modify: rebrand UI, connect to PortfolioAgent)
- `PROJECT_STEPS.md` (Update)
- `PROMPTS.md` (Update)

## 3. Risks & Mitigations

| Risk | Mitigation |
|---|---|
| DO class name mismatch | Keep exact class name `PortfolioAgent` across `wrangler.jsonc`, `server.ts`, and `app.tsx`. |
| Stale TypeScript types | Run `npx wrangler types` immediately after modifying `wrangler.jsonc`. |
| Breaking UI components | Keep Kumo component library, Streamdown, and Phosphor icons that starter already imports. |

## 4. Verification Steps

1. `npx wrangler types` regenerates `worker-configuration.d.ts` with `PortfolioAgent` binding.
2. `npm run typecheck` passes with 0 errors.
3. Verify starter demo tools are completely removed.
4. Git commit: `feat: llama 3.3 persona agent`.
